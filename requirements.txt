# Core framework
Django==6.0.2
asgiref==3.11.1  # Django dependency
sqlparse==0.5.5  # Django dependency

# Environment / configuration
python-dotenv==1.2.1

# Credential encryption
cryptography==46.0.5
cffi==2.0.0       # cryptography dependency
pycparser==3.0    # cffi dependency

# AI Models
openai==2.29.0            # LLM insights - OpenAI provider
anthropic==0.86.0         # LLM insights - Anthropic provider

# Postgres connection
psycopg2-binary==2.9.11   # PostgreSQL connector (native metadata extraction)

# Formatting
markdown==3.10.2

# Background tasks
celery[redis]==5.4.0
django-celery-beat==2.9.0
croniter==6.2.2

# Connectors / data sources
airbyte==0.53.2           # PyAirbyte adapter for 300+ sources; requires Python 3.12 (no 3.13 wheels), pulls pyarrow 21 + a large transitive tree
